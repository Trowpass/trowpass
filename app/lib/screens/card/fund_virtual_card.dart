import 'package:flutter/material.dart';

class FundVirtualCardScreen extends StatelessWidget {
  const FundVirtualCardScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(),
    );
  }
}
