import 'package:flutter/material.dart';

class LinearIndeterminateProgress extends StatelessWidget {
  final bool isLoaded;

  const LinearIndeterminateProgress({super.key, required this.isLoaded});

  @override
  Widget build(BuildContext context) {
    return const Center();
  }
}
